export enum ECanvasMode {
  NONE = "none",
  PRESSING = "pressing",
  SELECTION_NET = "selection_net",
  TRANSLATING = "translating",
  INSERTING = "inserting",
  RESIZING = "resizing",
  PENCIL = "pencil",
}

export enum ELayerType {
  Text,
  Note,
  Path,
  Squares,
  Circle,
}
